# .env - Backend local development only (do NOT commit to Git)

# MongoDB Atlas connection string
# Replace YOUR_PASSWORD with your actual password
# If password has special chars (@ / : ? # %), URL-encode them!
MONGO_URI=mongodb+srv://root:May5211535@cluster1.ndbx1yn.mongodb.net/jlabs_db?retryWrites=true&w=majority

# Local server port (optional - Express will use 8000 by default if not set)
PORT=8000

# Optional: for better debugging locally
NODE_ENV=development